# The WebSocket backend URL is auto-derived from the page host (see src/editor.js),
# so opening the app via localhost OR a LAN IP both work with no config.
#
# Optional overrides (uncomment if your backend uses a non-default port/host):
# VITE_WS_PORT=1234
# VITE_WS_URL=ws://your-host:1234
